// src/reducers/someReducer.js

const initialState = {
    // Defina o estado inicial aqui
  };
  
  const someReducer = (state = initialState, action) => {
    switch (action.type) {
      // Defina os casos para as ações aqui
      default:
        return state;
    }
  };
  
  export default someReducer;
  